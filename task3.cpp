#include <iostream>
using namespace std;

int main()
{
	cout << Is there a bug here ? "
		return 0;
}

/*
Запитання:
1. Чому ця програма дає збій (помилку компіляції)?
   Відповідь: Програма дає збій, тому що рядок "Is there a bug here?" не взято в подвійні лапки. У C++ рядкові літерали повинні бути оточені подвійними лапками (`"`), щоб компілятор міг їх правильно інтерпретувати. Без лапок `Is there a bug here?` розглядається як набір неоголошених ідентифікаторів, що призводить до помилки компіляції.
*/